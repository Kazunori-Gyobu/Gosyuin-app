<% provide :title, 'ユーザー一覧' %>
<div class="container">
    <h1>ユーザー一覧</h1>
    <ul class="users">
        <% @users.each do |user| %>
        <li>
            <%= image_tag user.avatar.to_s, :class => "avatar circle-avatar", :size => "100x100" %>
            <span class="user-name"><%= link_to user.name, user %></span><br />
            <span class="profile"><%= user.profile %></span>
        </li>
        <% end %>
    </ul>
    <%= paginate @users %>
</div>