<% provide :title, 'ユーザー一覧' %>
<h1>ユーザー一覧</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag current_user.avatar.to_s, :class => "avatar circle-avatar", :size => "100x100" %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
