<% provide :title, @stampbook.name %>
<div class="card col-sm-10 mx-auto mt-5 px-0">
  <span class="stampbook-name"><%= @stampbook.name %></span>
  <span class="stampbook"><%= image_tag @stampbook.cover.to_s, size: "300x200" %></span>
  <span>取得日<%= @stampbook.given_date %></span>
  <span>メモ<%= @stampbook.remarks %></span>
  <span><%= link_to "編集", edit_stampbook_path(@stampbook) %></span>
  <span><%= link_to "削除", @stampbook, method: :delete, data: { confirm: "登録されている御朱印も削除されます。本当に削除しますか？" } %></span>
</div>